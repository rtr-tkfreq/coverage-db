# Single all-in-one host by default (this box already runs postgres, nginx,
# postgrest and qgis together). Point this at real target host(s) instead
# once the pipeline is meant to run somewhere other than localhost.
[coverage_db]
localhost ansible_connection=local
